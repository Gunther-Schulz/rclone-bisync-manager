#!/bin/sh
# Run tests before commit. Install once: git config core.hooksPath githooks
set -e
cd "$(git rev-parse --show-toplevel)" || exit 1
if [ -x .venv/bin/python ]; then
    .venv/bin/python -m pytest tests/ -q
else
    python3 -m pytest tests/ -q
fi
